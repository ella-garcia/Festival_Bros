import matplotlib.pyplot as plt
from geopandas import GeoDataFrame as gdf
from shapely.geometry import Point
import pandas as pd

#Get map information as a list

#Open the shapefile (points)

#Open the shapefile (polygons (US States))

#Based on the list of cities given, select the locations from the shapefile

#Display